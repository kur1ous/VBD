Happy Birthday, My Love <3.

Quick setup
- Edit `main.js` and update `config.name`, `config.fromName`, and `config.birthday`
- Customize the lists in `config.reasons` and `config.moments`
- Deploy with GitHub Pages by selecting the `main` branch in repo settings
